export enum EventsMessagePatterns {
  CREATE = 'event.create',
  UPDATE = 'event.update',
  DELETE = 'event.delete',
  FIND_MANY = 'event.findMany',
  FIND_ONE = 'event.findOne',
  UPLOAD_IMAGE = 'event.images.upload',
}
