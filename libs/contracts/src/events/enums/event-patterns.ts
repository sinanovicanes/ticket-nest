export enum EventsEventPatterns {
  CREATED = 'event.created',
  UPDATED = 'event.updated',
  DELETED = 'event.deleted',
  ADD_IMAGE = 'event.images.add',
  REMOVE_IMAGE = 'event.images.remove',
  REMOVE_IMAGE_BY_URL = 'event.images.removeByURL',
}
